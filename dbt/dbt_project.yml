# Name your project! Project names should contain only lowercase characters
# and underscores. A good package name should reflect your organization's
# name or the intended use of these models
name: 'streamcommerce'
version: '1.0.0'
config-version: 2

# This setting configures which "profile" dbt uses for this project.
profile: 'streamcommerce'

# These configurations specify where dbt should look for different types of files.
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"  # directory which will store compiled SQL files
clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"

# Configuring models
models:
  streamcommerce:
    # For staging models, configure them to use the existing staging tables as sources
    staging:
      +materialized: ephemeral  # Do not materialize staging models
    core:
      +materialized: table

# Define variables
vars:
  prod_dataset: "prod_streamcommerce"  # Specify the production dataset name
